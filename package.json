{
  "name": "smart-pet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "pnpm -r --filter './packages/**' run build",
    "test": "pnpm -r --filter './packages/**' run test",
    "typecheck": "pnpm -r --filter './packages/**' run typecheck",
    "dev": "pnpm --filter @smartpet/ui dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}